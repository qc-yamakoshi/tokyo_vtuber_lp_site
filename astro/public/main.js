const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll(".reveal").forEach((el)=>observer.observe(el));const btn=document.querySelector(".menu-button");const menu=document.querySelector(".mobile-menu");if(btn&&menu){btn.addEventListener("click",()=>{const open=menu.classList.toggle("open");menu.hidden=!open;btn.setAttribute("aria-expanded",String(open))});menu.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{menu.classList.remove("open");menu.hidden=true;btn.setAttribute("aria-expanded","false")}))}
;document.querySelectorAll(".embed-shell[data-embed-url]").forEach((shell)=>{const url=(shell.dataset.embedUrl||"").trim();if(/^https:\/\//.test(url)){const frame=shell.querySelector("iframe");if(frame){frame.src=url;shell.classList.add("is-ready")}}});
;const backToTop=document.querySelector('.back-to-top');if(backToTop){const toggleTopButton=()=>{backToTop.classList.toggle('visible',window.scrollY>420)};toggleTopButton();window.addEventListener('scroll',toggleTopButton,{passive:true});}
